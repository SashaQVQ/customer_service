# 事实校验护栏（Fact Check Guardrail）架构

## 整体流程

```
用户提问
  │
  ▼
┌─────────────────────────────────────┐
│          MChat RAG 引擎              │
│  ┌──────────┐    ┌───────────────┐  │
│  │ 知识库检索 │───▶│ LLM 生成回答   │  │
│  └──────────┘    └───────┬───────┘  │
└──────────────────────────┼──────────┘
                           │
                           ▼
                  ┌─────────────────┐
                  │  事实校验护栏     │  ← 自研模块
                  └────────┬────────┘
                           │
            ┌──────────────┼──────────────┐
            ▼              ▼              ▼
      ┌──────────┐  ┌──────────┐  ┌──────────┐
      │ 正则提取  │  │ KB 上下文 │  │ 精确比对  │
      │ 实体列表  │  │ 实体列表  │  │          │
      └────┬─────┘  └────┬─────┘  └────┬─────┘
           │             │             │
           └─────────────┼─────────────┘
                         │
                         ▼
                  ┌─────────────┐
                  │  全部匹配？   │
                  └──────┬──────┘
                     ╱        ╲
                   是          否
                   ╱             ╲
                  ▼               ▼
          ┌────────────┐  ┌────────────────┐
          │ ✅ 已核实    │  │ ⚠️ 含未验证信息  │
          │ (绿色标签)   │  │ (琥珀色标签)     │
          └────────────┘  └────────────────┘
```

## 关键设计决策

| 决策点 | 选择 | 理由 |
|--------|------|------|
| 校验粒度 | 实体级别（快递名、政策关键词、价格数字） | 电商客服出错代价最大的是实体，不是语气 |
| 比对方式 | 精确匹配 | 不做语义相似度——延迟低，可解释性强 |
| 是否二次 LLM | 不 | 避免延迟翻倍 + 成本翻倍，对小商家不现实 |
| 检索失败时 | 护栏静默，不展示标签 | 已知边界——护栏依赖 RAG 先命中 |

## Badcase 驱动链

```
发现 Badcase：中通 → 中通 + 圆通（编造）
  │
  ├─ 分析：检索返回正确（中通），LLM 生成时自己加了圆通
  │
  ├─ 结论：需要在检索正确的前提下，校验生成的实体是否都在 KB 里
  │
  ├─ 方案：正则提取 → KB 比对 → 标注 ✅/⚠️
  │
  └─ 边界：前提是 RAG 要命中。检索失败时护栏失声——加强检索覆盖是正解
```
