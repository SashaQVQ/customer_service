# 系统架构

> v1.0 | 2026-07-06

## 整体架构

```
┌─────────────────────────────────────────────┐
│                  买家端                       │
│  Web Widget (JS embed)  │  微信公众号         │
└──────────────────┬──────────────────────────┘
                   │ HTTP/WebSocket
┌──────────────────▼──────────────────────────┐
│              MChat AI 引擎                    │
│  ┌──────────┐ ┌──────────┐ ┌──────────────┐ │
│  │ Bot      │ │ RAG      │ │ Workflow     │ │
│  │ Engine   │ │ 混合检索  │ │ 编排 (Beta)  │ │
│  └──────────┘ └──────────┘ └──────────────┘ │
│                                              │
│  ★ 新增：人工坐席路由  ★ 新增：CSAT+SLA     │
│  ┌──────────┐ ┌──────────┐                  │
│  │ Agent    │ │ Feedback │                  │
│  │ Routing  │ │ + SLA    │                  │
│  └──────────┘ └──────────┘                  │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│              数据层                           │
│  MySQL 8.0 │ DeepSeek API │ 硅基流动 Embedding│
└─────────────────────────────────────────────┘
```

## 模块交互

### 买家咨询 → AI 回答 → 转人工

```
买家消息 → MChat Bot Engine
  → RAG 检索 → LLM 生成回复
  → EscalationRule 检测：
      ├─ 命中关键词 "转人工"/"投诉" → 升级
      ├─ 连续 2 轮无匹配 → 升级
      └─ 正常 → 返回 AI 回复
  → 升级：通知 AgentSession 中最空闲的坐席
```

### 满意度反馈流程

```
AI 回复 → Widget 显示赞/踩按钮
  → 用户点踩 → 弹出原因选择器
  → POST /api/feedback → 存入 feedbacks 表
  → Dashboard CSAT 指标实时更新
```

## 关键依赖

| 组件 | 部署方式 | 端口 |
|------|----------|------|
| MySQL | Docker | 3307 |
| MChat Backend | Python/uvicorn | 3001 |
| MChat Frontend | Vite dev server | 5173 |
| DeepSeek API | 云服务 | - |
| 硅基流动 Embedding | 云服务 | - |
